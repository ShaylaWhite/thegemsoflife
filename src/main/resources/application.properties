# Server port (default is 8080, change if needed)
server.port=8080

# Spring application name (optional)
spring.application.name=gemsoflife-game

# Logging configuration (optional, default is INFO level)
logging.level.root=INFO

# Profile-specific properties (optional)
# spring.profiles.active=dev

# You can add additional configuration for the embedded server or other settings as needed
